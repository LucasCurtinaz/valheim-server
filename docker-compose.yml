services:
  valheim:
    image: lloesche/valheim-server
    container_name: valheim-server
    restart: unless-stopped

    environment:
      SERVER_NAME: Curtinaz
      WORLD_NAME: expuxadores
      SERVER_PASS: expuxadores
      SERVER_PUBLIC: "true"

    volumes:
      - ./config:/config